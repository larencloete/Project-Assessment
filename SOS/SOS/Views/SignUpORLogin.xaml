<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SOS.Views.SignUpORLogin" 
             Title="SignUpORlogin">

    <StackLayout Margin="10">

        <Button Text="Sign Up" Command="{Binding NavigateCommand }"
                Opacity="90"
                BackgroundColor="Gold"
                TextColor="Black"
                VerticalOptions="Center"  />

        <Label Text="OR" TextColor="Black" FontSize="Large" HorizontalTextAlignment="Center" />

        <Button Text="Login" Command="{Binding NavigateLoginCommand }"
                VerticalOptions="Center"  />

    </StackLayout>
    
</ContentPage>